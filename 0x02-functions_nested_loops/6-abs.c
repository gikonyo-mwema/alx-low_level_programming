#include "main.h"

/**
 * _abs - print lowercase alphabets
 *
 * @n: number ASCII
 * Return: 1 lower and 0 otherwise
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
