# Variables
CC = gcc
SRC = $(wildcard *.c)
OBJ = $(patsubst %.c,%.o,$(SRC))
NAME = school

#Default rule
all: $(NAME)

# Rule to build the executable
$(NAME): $(OBJ)
	$(CC) -o $@ $^

# Rule to build object files
%.o: %.c
	$(CC) -c $< -o $@

# Rule to clean up
clean:
	rm -f $(OBJ) $(NAME)
